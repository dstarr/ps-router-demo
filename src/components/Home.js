import React from 'react';

const Home = () => {

    let imgStyle = {
        marginTop: 50
    };

    return(
        <img style={imgStyle} src={'/assets/codelife.png'} />
    );
};

export default Home;